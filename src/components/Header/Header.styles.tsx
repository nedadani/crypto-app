import styled from "styled-components";

const Nav = styled.header`
    width: 100%;
    height: 60px;
`;

const Logo = styled.img`
    height: 40px;
    padding: 10px 15px;
`;

export { Nav, Logo };
